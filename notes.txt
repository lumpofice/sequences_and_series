in all py scripts, logging gives an update_title_pos response

Before a function can be considered "completed": Functionality, F, which is user input, has been tested, Legend, L, which is legend output and figure output alignment, across all limits, lims, which are all combinations of upper and lower bounds for all variables a, x, and k, and pedagogy in jupyter file exists.

Complete:
u_nx_over_one_plus_nx_squared() 
u_nx_over_one_plus_nx()
